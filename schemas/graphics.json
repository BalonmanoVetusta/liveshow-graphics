{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "bgColor": {
      "type": "string",
      "title": "Background Color",
      "description": "Background color of the body",
      "default": "#00ff00"
    },
    "advertising": {
      "type": "boolean",
      "default": true,
      "title": "Advertising",
      "description": "If advertising must be showed"
    },
    "advertisingTime": {
      "type": "number",
      "default": 10,
      "title": "Time to change",
      "description": "Time in seconds that a banner is showed before change it if more than one banner is available"
    },
    "music": {
      "type": "boolean",
      "default": false,
      "title": "Music",
      "description": "If music must be played"
    },
    "spot": {
      "type": "boolean",
      "default": false,
      "title": "Play Spot",
      "description": "If a spot must be played"
    },
    "localShield": {
      "type": "string",
      "default": null,
      "title": "Local Team Shield",
      "description": "Local Team shield. Must be a full URL or a path relative to the app folder"
    },
    "visitorShield": {
      "type": "string",
      "default": null,
      "title": "Visitor Team Shield",
      "description": "Visitor Team shield. Must be a full URL or a path relative to the app folder"
    }
  }
}
