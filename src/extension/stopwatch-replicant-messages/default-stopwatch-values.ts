import { Stopwatch } from "types/schemas/stopwatch";

const defaultStopwatchValues = {
  startTime: 0,
  offset: 0,
  backwards: false,
  limit: 0,
  periodTime: 0,
} as Stopwatch;

export default defaultStopwatchValues;
